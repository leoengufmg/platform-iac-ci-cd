# Terraform Structure

## Environments
- `environments/dev` simple, local-state example
- `environments/prod` advanced, remote-state example

## Modules
- `modules/databricks-workspace` workspace configuration stubs
- `modules/iam-roles` IAM role templates